[project]
name = "sceneit-ingestion-proof"
version = "0.1.0"
description = "Flask and Twelve Labs one-video ingestion, search, and YouTube alignment proof"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.2.0",
    "httpx>=0.28.1",
    "psycopg[binary]>=3.3.5",
    "pydantic>=2.13.5",
]
